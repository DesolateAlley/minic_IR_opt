#include <stdio.h>
#include <time.h>
int main()
{
    int start,end;
    double use;
    start=clock();
    int a,b,c;
    for(int i=0;i<1e9;i++)
    {
        b=i+1;
        a=
        c=b+c;
    }
    printf("%d",c);
    end=clock();
    printf("Total time taken: %d us\n", end-start);
}